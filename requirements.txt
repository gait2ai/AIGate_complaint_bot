# Telegram Bot
python-telegram-bot>=20.0,<21.0

# HTTP Client for AI Handler
aiohttp>=3.8.0,<4.0.0

# YAML Configuration
PyYAML>=6.0,<7.0

# Timezone Handling
pytz>=2023.3

# -----------------------------------------------------------------------------
# Optional: Dependencies for Gmail API (for critical email notifications)
# Uncomment these lines if you re-enable the email sending functionality.
# Make sure you also have 'gmail_credentials.json' and 'token.json' configured
# in 'app/config/' and the Gmail API enabled in your Google Cloud project.
# -----------------------------------------------------------------------------
# google-api-python-client>=2.0.0,<3.0.0
# google-auth>=2.0.0,<3.0.0
# google-auth-oauthlib>=0.7.0,<1.0.0

# Optional: For asynchronous file I/O in CacheManager
# If you install this, CacheManager will use it. Otherwise, it will use synchronous I/O.
# aiofiles>=23.1.0,<24.0.0